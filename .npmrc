# Ensure optional dependencies are installed
optional=true

# Allow platform-specific binaries to be installed
target_platform=auto

# Ensure proper resolution of platform-specific packages
platform=auto
